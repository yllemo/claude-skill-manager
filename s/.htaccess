<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^([A-Za-z0-9_\-]+\.skill)$ index.php?file=$1 [L,QSA]
</IfModule>

# Zip är redan komprimerat — undvik gzip som korrumperar JSZip
<IfModule mod_deflate.c>
    SetEnv no-gzip 1
</IfModule>

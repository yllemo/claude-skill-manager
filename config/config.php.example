<?php
// Protected from direct HTTP access
if (isset($_SERVER['SCRIPT_FILENAME']) && realpath($_SERVER['SCRIPT_FILENAME']) === __FILE__) {
    http_response_code(403); exit;
}

return [

    // ── PASSWORD ────────────────────────────────────────
    // Legacy: används endast vid första migrering till config/users.php (admin-konto).
    // Nya installationer: skapa config/users.php via inställningar eller låt appen migrera automatiskt.
    'password' => 'admin123',

    // ── SESSION ─────────────────────────────────────────
    // How long login lasts (seconds). Default: 1 month.
    'session_lifetime' => 2592000,

    // ── APP ─────────────────────────────────────────────
    'app_name' => 'Skill Manager',

    // Tillåtna filtyper i .skill-arkiv: se config/files.php (kopiera från files.php.example)

    // Åtkomst & synlighet: se config/settings.php (kopiera från settings.php.example)
    // Användarkonton (ACL): se config/users.php (skapas automatiskt från password ovan)

];